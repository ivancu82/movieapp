import { PathApiWebPipe } from './path-api-web.pipe';

describe('PathApiWebPipe', () => {
  it('create an instance', () => {
    const pipe = new PathApiWebPipe();
    expect(pipe).toBeTruthy();
  });
});
